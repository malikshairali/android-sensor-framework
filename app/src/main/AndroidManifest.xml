<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sensors">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SensorsHandson">
        <activity android:name=".AvailableSensors"></activity>
        <activity android:name=".Barometer" />
        <activity android:name=".Magnetometer" />
        <activity android:name=".Gyroscope" />
        <activity android:name=".Accelerometer" />
        <activity android:name=".LightSensor" />
        <activity android:name=".ProximitySensor" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2386761694406298~6447605969" />

    </application>

</manifest>